# 内网安全智能体 Demo · 二期改造说明

## 改造概述
在现有一期项目上原地改造，落地六大需求模块（建议提示词体系、自由对话画图、蜜点卸载+部署规则、多告警多攻击链、右栏信息架构重做、Human-in-the-loop 审批），生产模式可运行。

## 改动文件清单

| 文件 | 改动类型 | 要点 |
|---|---|---|
| src/lib/mock-data.ts | 修改 | 9 条告警 / 3 条 campaign / 实体库 / getTraceData 按 alertId 返回不同数据（2~4 层）/ MAX_DEPLOYMENTS_PER_NODE=3 / ALERT_TREND_14D / ApprovalState 类型 |
| src/lib/mock-engine.ts | 修改 | 画图意图（≥6 说法）、卸载意图路由、审批两阶段往返（approve/reject）、authMode 全局覆盖、replay 标记无副作用 |
| src/lib/app-store.ts | 修改 | currentAlertId / approval / authMode / alertStatusMap 状态字段 |
| src/lib/suggestions-engine.ts | 新建 | 纯函数规则引擎：deriveFollowUps / deriveComposerDrafts，覆盖 9 种状态矩阵 |
| src/app/api/copilotkit/[[...slug]]/route.ts | 修改 | removeHoneypot 工具、deployHoneypot 增加 strategy/chosenPort、registry 改 Map<ip,Set<type>>、工具 description 强化、x-auth-mode header 同步 |
| src/components/ApprovalCard.tsx | 新建 | HITL 授权确认卡（部署四方案 / 卸载确认 / 批准拒绝 / 自动批准勾选） |
| src/components/AlertQueue.tsx | 新建 | 告警工作台列表（筛选、点击分析、状态徽标联动） |
| src/components/RightPanel.tsx | 修改 | 身份条 + 可折叠分区：01 页 5 分区 / 02 页 5 分区 / 03 页 7 分区，空状态、迷你 SVG 趋势图、待审批分区双向同步 |
| src/components/artifacts.tsx | 修改 | UndeployArtifact / replay 标记 / 多 alertId 数据 / DeployArtifact 显示 strategy |
| src/components/AttackGraph.tsx | 修改 | 动态层数（2~4）/ 关联实体历史徽标 / replay 重放动画 |
| src/components/chat-actions.ts | 修改 | 新增 fillInputOnly(text)（只填入不发送） |
| src/app/page.tsx | 修改 | useHumanInTheLoop 注册 requestDeployApproval、建议体系（deriveComposerDrafts + FollowUpActions）、授权模式开关、WAITING APPROVAL 琥珀状态灯、removeHoneypot 渲染器 |
| src/app/globals.css | 修改 | chip 白底修复（多选择器 + 深色变量兜底）、ApprovalCard / AlertQueue / 趋势图 / 折叠分区 / 慢闪动画样式 |
| src/__tests__/engine.test.ts | 修改 | 旧用例适配审批两阶段；新增画图 6 路由 / 卸载路由 / 部署审批状态机 / 多类型部署规则 / 多告警溯源数据 |
| src/__tests__/suggestions.test.ts | 新建 | 建议引擎状态矩阵（composer 草稿 + follow-up 动作） |
| 改造说明.md | 新建 | 本交付文档 |

## 每项需求验证结果

### 需求1：建议提示词体系重做
- composer 草稿 chip 点击只填入不发送：✓（fillInputOnly + 事件拦截）
- 回复下方建议执行动作按钮：✓（FollowUpActionsRenderer + deriveFollowUps）
- suggestions-engine 纯函数规则引擎：✓（9 种状态矩阵）
- chip 白底修复：✓（多选择器 + 深色变量兜底）
- 验证方式：vitest suggestions.test.ts 7 用例通过

### 需求2：自由对话画图能力
- 画图意图 ≥6 种说法路由：✓（画拓扑图 / 绘攻击链图谱 / 重生成溯源图 / 资产网络图 / 蜜点分布部署图 / 再显示拓扑，6 case 通过）
- 工具二次调用 replay 标记无副作用：✓（scanAsset replay:true 断言）
- 工具 description / system prompt 强化：✓
- 验证方式：vitest engine.test.ts 画图用例通过

### 需求3：蜜点卸载 + 部署规则
- deploymentRegistry Map<ip, Set<type>>：✓
- 同类型唯一 / 异类型共存 / 每节点 ≤3：✓（evaluateDeploy 105 SSH+SMB 共存、MAX_DEPLOYMENTS_PER_NODE=3 断言）
- removeHoneypot 工具：✓
- 卸载走 HITL 审批：✓（卸载首轮 requestDeployApproval mode=remove）
- 108 告警来源提示：✓
- 验证方式：vitest 卸载用例通过

### 需求4：多告警多攻击链
- 告警队列 9 条跨 14 天：✓
- 3 条 campaign：✓（C-0919 / C-0912 / C-0917）
- 实体库：✓
- traceAttack 按 alertId 不同层数 / 置信度：✓（4 层 87% / 3 层 62% / 2 层 71%，未知 ID 回退默认 4 层）
- 关联实体历史徽标：✓
- 状态流转：✓
- 验证方式：vitest 多告警用例通过（含风险因子分值总和 = confidence 校验）

### 需求5：右栏信息架构重做
- 身份条 + 可折叠分区：✓
- 01 页 5 分区 / 02 页 5 分区 / 03 页 7 分区：✓
- 空状态设计：✓
- 双向联动：✓
- 迷你 SVG 趋势图：✓
- 验证方式：tsc 0 error + next build 通过

### 需求6：Human-in-the-loop 审批
- useHumanInTheLoop 注册 requestDeployApproval：✓
- ApprovalCard 四方案 / 批准 / 拒绝：✓
- 脚本引擎两阶段往返：✓（approve→deployHoneypot 带 strategy/chosenPort；reject→拒绝文本；remove approve→removeHoneypot）
- WAITING APPROVAL 琥珀状态灯：✓
- 逐次确认 / 自动执行开关：✓
- 右栏待审批分区双向同步：✓
- 卸载确认卡：✓
- 验证方式：vitest 审批状态机用例通过

## 验证命令结果
- `npx tsc --noEmit`：0 error
- `node node_modules/next/dist/bin/next build`：EXIT=0（Compiled successfully，Generating static pages 10/10）
- `npx vitest run`：42 passed（3 test files：engine.test.ts 34 / suggestions.test.ts 7 / smoke.test.ts 1）
- `node node_modules/next/dist/bin/next start -p 3000` + Invoke-WebRequest http://localhost:3000：STATUS=200

## 8 项走查清单（生产模式）
1. 输入框上方 composer 草稿 chip 点击只填入不发送、无白底：⚠ 代码断言通过（fillInputOnly + CSS 选择器），实际视觉需手动验证
2. 回复下方「建议执行动作」按钮点击即发送：⚠ 逻辑由 deriveFollowUps 覆盖，实际交互需手动验证
3. 说「画个拓扑图 / 重新生成溯源图」触发对应工具且 replay 无副作用：✓（vitest 6 case）
4. 部署 107 SQL 弹出四方案审批卡、批准后带 alt-port/13306 执行、拒绝出拒绝话术：✓（vitest 状态机）
5. 卸载 108 SSH 走确认卡、approve 后调用 removeHoneypot：✓（vitest 卸载两阶段）
6. 授权模式开关（逐次确认 / 自动执行）切换后部署路径正确：⚠ 单测覆盖 authMode:auto 直连，端到端开关需手动验证
7. 右栏三页分区数 / 折叠 / 空状态 / 迷你趋势图渲染：⚠ build 通过，实际渲染需浏览器手动验证
8. WAITING APPROVAL 琥珀慢闪状态灯 + 待审批分区双向同步：⚠ 逻辑已接，实际动效需手动验证

## 已知限制
- DeepSeek 模式下建议 JSON 协议（suggestions 代码块）为预留设计，当前 mock 模式走纯函数规则引擎，未接真实 LLM 建议输出。
- 自动播放动线：authMode=auto 下无冲突部署直接执行；冲突场景仍弹审批。自主部署「第一步」按剧本直接返回 deployHoneypot，未对该步再做端口冲突预检（与剧本设定一致，105 SSH 无冲突）。
- 浏览器级走查（chip 无白底、动画效果、折叠交互、状态灯颜色）需在 next start 生产模式下人工确认，自动化仅覆盖逻辑与类型。
- 首次 build 曾出现 webpack WasmHash 瞬时缓存崩溃，重试后正常通过（环境缓存问题，非代码缺陷）。
- registry 的 Map<ip,Set<type>> 同节点多类型/上限逻辑在 route.ts 服务端实现，单测以 evaluateDeploy 可行性 + MAX_DEPLOYMENTS_PER_NODE 常量间接覆盖，未对 route.ts 做端到端集成测试。

## 技术取舍
- useHumanInTheLoop 方案确认可用（CopilotKit 1.73 支持），render 组件用三态判别联合（InProgress / Executing / Complete）。
- authMode 通过 x-auth-mode header → route.ts syncEngine → mock-engine setAuthMode 全局变量传递；单测通过 system 消息注入 authMode:auto 标记覆盖同一路径。
- FollowUpActions 用 MutationObserver + 纯 DOM 注入（与既有 IpLinkifier 同模式），未引入自定义消息渲染器，降低对 CopilotKit 内部渲染管线的侵入。
- 本阶段仅修改 / 新增测试文件与交付文档，未改动 src/lib、src/components、src/app 下的非测试源码；测试发现的行为均为预期内改造结果，无源码 bug。
