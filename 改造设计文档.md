# 内网安全智能体 Demo · 二期改造设计文档

> 工作过程产物，不作为交付确认。所有业务口径以 `安全智能体Demo-二期改造提示词.md` 为准。

## 0. API 调研结论（CopilotKit 1.73 /v2）

### useHumanInTheLoop — 确认可用

```ts
useHumanInTheLoop<T>(tool: ReactHumanInTheLoop<T>, deps?)
```

`ReactHumanInTheLoop<T>` = `Omit<FrontendTool<T>, "handler">` + `render` 组件。

render 组件收到**判别联合**三态：
| 状态 | args | respond | 含义 |
|---|---|---|---|
| `InProgress` | `Partial<T>` | `undefined` | 工具刚被调用，参数还在解析 |
| `Executing` | `T` | `(result: unknown) => Promise<void>` | **审批卡渲染于此，用户操作后调 respond** |
| `Complete` | `T` | `undefined` | respond 已回灌，结果作为 tool result |

**关键**：respond 的返回值会作为该前端工具的 tool result，进入下一轮 `/chat/completions` 的 messages（role=tool），脚本引擎可在第二次请求中识别并继续链式调用 deployHoneypot。

**与 renderToolCalls 共存**：HITL 工具不走 renderToolCalls，它由 CopilotKit 内部识别为 frontend tool 并直接渲染 render 组件。服务端工具（scanAsset/deployHoneypot/traceAttack/removeHoneypot）继续走 renderToolCalls。两者互不冲突。

**Provider 注册**：`useHumanInTheLoop` 在组件内调用即可注册，无需在 Provider props 上额外声明（Provider 有 `humanInTheLoop?: ReactHumanInTheLoop[]` 但 hook 方式更灵活）。

### 脚本引擎两阶段审批时序

```
用户: "在 107 部署 SQL 蜜点"
  → POST /chat/completions (messages=[user])
  → 脚本引擎 plan(): 部署意图 + 端口冲突 → 返回 tool_calls=[requestDeployApproval]
  → finish_reason: "tool_calls"
  → CopilotKit 识别为 frontend HITL tool → 聊天挂起 → 渲染 ApprovalCard
  → 用户选方案A + 批准 → respond({decision:"approve", strategy:"alt-port", port:13306})
  → POST /chat/completions (messages=[user, assistant(tool_calls), tool(result)])
  → 脚本引擎 plan(): 识别最后 tool result 是 requestDeployApproval 的 approve
    → 返回 tool_calls=[deployHoneypot(ip, type, strategy, chosenPort)]
  → CopilotKit 执行服务端 deployHoneypot → 返回部署卡数据
  → POST /chat/completions (messages=...+tool(deployResult))
  → 脚本引擎 plan(): 识别 deployHoneypot 结果 → 返回收尾文本
```

拒绝路径：respond({decision:"reject"}) → 脚本引擎返回拒绝话术文本，不调 deployHoneypot，系统状态无变化。

---

## 1. 文件级改动清单

### 新增文件
| 文件 | 职责 |
|---|---|
| `src/lib/suggestions-engine.ts` | 纯函数规则引擎：deriveFollowUps / deriveComposerDrafts，覆盖9种状态矩阵 |
| `src/components/ApprovalCard.tsx` | HITL 授权确认卡（部署四方案 / 卸载确认 / 批准拒绝 / 自动批准勾选） |
| `src/components/AlertQueue.tsx` | 告警工作台列表（筛选、点击分析、状态徽标联动） |
| `改造说明.md` | 交付说明（改动清单 + 验证结果 + 已知限制） |

### 修改文件
| 文件 | 改动要点 |
|---|---|
| `src/lib/mock-data.ts` | 告警队列8-10条、3 campaign、实体库、traceAttack按alertId返回不同数据、deploymentRegistry多类型支持、removeHoneypot相关数据 |
| `src/lib/mock-engine.ts` | 画图意图(≥6说法)、卸载意图、审批两阶段往返、建议JSON协议、replay标记无副作用 |
| `src/lib/app-store.ts` | currentAlertId、approvalState、每节点多诱饵(deployments模型不变但addDeployment支持多条)、authMode、alertStatusMap、miniTrend数据 |
| `src/app/api/copilotkit/[[...slug]]/route.ts` | removeHoneypot工具、deployHoneypot增加strategy/chosenPort参数、registry改Map<ip,Set<type>>、工具description强化(画图必须调工具)、系统prompt强化 |
| `src/components/artifacts.tsx` | UndeployArtifact(卸载状态卡)、TraceArtifact支持多alertId/replay、DeployArtifact支持strategy显示 |
| `src/components/AttackGraph.tsx` | 支持不同告警层数(2-4层)、关联实体历史事件徽标、replay重放动画 |
| `src/components/RightPanel.tsx` | 全面重做：身份条+可折叠分区，01页5分区/02页5分区/03页7分区，空状态，迷你SVG趋势图，待审批分区双向同步 |
| `src/components/chat-actions.ts` | 新增 fillInputOnly(text) 函数（只填入不发送，用于composer草稿chip） |
| `src/app/page.tsx` | SuggestionsConfigurator改用deriveComposerDrafts+fillInputOnly、新增FollowUpActions(回复下方按钮)、useHumanInTheLoop注册requestDeployApproval、授权模式开关(逐次确认/自动执行)、状态灯第三态WAITING APPROVAL、ApprovalCard渲染 |
| `src/app/globals.css` | chip白底修复(精准选择器)、ApprovalCard样式、AlertQueue样式、趋势图样式、分区折叠样式、WAITING APPROVAL灯动画 |
| `src/__tests__/engine.test.ts` | 新增：画图意图6种路由、卸载路由、同节点多类型/上限、审批状态机、traceAttack多告警、建议引擎状态矩阵；旧用例同步更新 |

---

## 2. 数据结构设计

### 2.1 Store 新增字段

```ts
interface AppState {
  // ... 现有字段保留 ...
  currentAlertId: string | null;        // 当前分析的告警ID
  alertStatusMap: Record<string, AlertStatus>;  // 告警状态流转（待研判/研判中/已闭环/误报）
  approval: ApprovalState | null;        // 当前待审批（null=无待决）
  authMode: "confirm" | "auto";          // 操作授权模式
  autoApproveTypes: Set<HoneypotType>;  // 本次会话已勾选自动批准的类型
}

interface ApprovalState {
  id: string;                    // 唯一审批ID
  mode: "deploy" | "remove";
  ip: string;
  type: HoneypotType;
  conflict?: { port: number; process: string; pid: number };
  options: ApprovalOption[];     // 四方案
  selectedIndex: number;
  status: "waiting" | "approved" | "rejected";
  toolCallId?: string;           // 关联的 HITL toolCallId
}

interface ApprovalOption {
  id: "alt-port" | "stop-service" | "reverse-proxy" | "other-node";
  label: string;
  pros: string;
  cons: string;
  impact: "低" | "中" | "高";
  recommended?: boolean;
  port?: number;
  targetIp?: string;
}
```

### 2.2 告警数据结构

```ts
interface Alert {
  id: string;                    // ALERT-2026-0919-001
  time: string;                  // 2026-09-19 02:14:33
  shortTime: string;
  decoyIp: string;
  decoyName: string;
  decoyType: HoneypotType;
  decoyPort: number;
  title: string;
  severity: "critical" | "high" | "medium" | "low";
  behavior: string[];
  status: "待研判" | "研判中" | "已闭环" | "误报";
  campaignId: string;            // C-0919 / C-0912 / C-0917
  confidence?: number;           // 研判后回填
  sourceIp: string;
  sourceLabel: string;           // 内网段 / 外网模拟
}

interface Campaign {
  id: string;
  name: string;
  severity: "critical" | "high" | "medium";
  status: "待研判" | "研判中" | "已闭环";
  description: string;
  alertIds: string[];
}

interface Entity {
  type: "ip" | "account" | "ioc" | "ttp";
  value: string;
  label: string;
  isJumpHost?: boolean;
  historyCount: number;          // 历史告警出现次数
  relatedAlertIds: string[];
}
```

### 2.3 traceAttack 按 alertId 返回不同数据

```ts
// 函数签名
function getTraceData(alertId: string): {
  alert: Alert;
  layers: TraceLayer[];          // 不同告警层数不同(2-4)
  timeline: TimelineEvent[];
  chain: string[];
  mitre: MitreTechnique[];
  riskFactors: RiskFactor[];     // 分值不同但总和=confidence
  affected: { hosts: number; accounts: number; decoys: number };
  responses: ResponseAction[];
  verdict: { confidence: number; summary: string };
}
```

三条主告警数据差异：
- **ALERT-2026-0919-001 (C-0919, critical)**: 4层溯源，置信度87%，完整攻击链
- **ALERT-2026-0912-003 (C-0912, medium, 已闭环)**: 2-3层，置信度71%，简单扫描
- **ALERT-2026-0917-002 (C-0917, high, 研判中)**: 3层，置信度62%，行为推断占比高

### 2.4 deploymentRegistry 改造

```ts
// 旧: Map<ip, HoneypotType>
// 新: Map<ip, Set<HoneypotType>>
const deploymentRegistry = new Map<string, Set<HoneypotType>>([
  ["192.168.1.108", new Set(["SSH"])],
]);

// 规则:
// - 同节点同类型: 幂等返回"已在运行"
// - 同节点异类型: 共存(端口预检通过)
// - 每节点上限: 3个(Set.size >= 3时拒绝新类型)
// - 不支持设备: 全部拒绝
// - 网关: 仅WEB
```

---

## 3. 工具 Schema

### 3.1 现有工具修改

**deployHoneypot** — 新增参数：
```ts
parameters: z.object({
  ip: z.string(),
  type: z.enum(["SSH","SQL","WEB","SMB"]),
  strategy: z.enum(["direct","alt-port","stop-service","reverse-proxy","other-node"]).optional(),
  chosenPort: z.number().optional(),
})
```

**traceAttack** — alertId 已有，现在按ID返回不同数据。

### 3.2 新增服务端工具

**removeHoneypot**:
```ts
parameters: z.object({
  ip: z.string().describe("目标节点IP"),
  type: z.enum(["SSH","SQL","WEB","SMB"]).optional().describe("省略则移除该节点全部诱饵"),
})
// 返回: { removed: [{type, port}], releasedPorts: number[], archivedLogs: number, status }
```

### 3.3 前端 HITL 工具

**requestDeployApproval** (useHumanInTheLoop 注册):
```ts
{
  name: "requestDeployApproval",
  description: "部署/卸载前请求用户授权确认。端口冲突时必须调用，展示四方案供用户选择。",
  parameters: z.object({
    mode: z.enum(["deploy","remove"]),
    ip: z.string(),
    type: z.enum(["SSH","SQL","WEB","SMB"]),
    conflict: z.object({ port: z.number(), process: z.string(), pid: z.number() }).optional(),
  }),
  render: ApprovalCard,  // 组件内根据 mode 渲染四方案或卸载确认
}
```

---

## 4. 建议引擎规格 (suggestions-engine.ts)

```ts
interface FollowUpAction {
  id: string;
  label: string;
  prompt: string;
  kind: "primary" | "ghost";
}

deriveFollowUps(state: AppState, lastAssistantText: string, lastTool?: string): FollowUpAction[]
deriveComposerDrafts(state: AppState): string[]
```

状态矩阵（每条2-3个，去重）：
| 状态 | 回复下方动作 |
|---|---|
| 体检前 | 「对网段做内网体检」(主)、「你能做什么？」(ghost) |
| 体检完成 | 「Agent自主部署蜜点」(主)、「分析最近一条蜜点告警」(ghost)、「高危资产有哪些？」(ghost) |
| 部署失败/不支持 | 「换一个节点部署」(主)、「查看不能部署原因」(ghost) |
| 端口冲突待审批 | 不出现新动作(审批卡置顶) |
| 审批拒绝后 | 「重新选择部署方案」(主) |
| 同节点已有同类型 | 「卸载现有SSH诱饵」(主)、「改部署其他类型」(ghost) |
| 告警队列有待研判 | 「分析下一条待研判告警」(主) |
| 归因进行中 | 不打扰 |
| 归因完成 | 「生成处置工单」(主)、「查看关联历史事件」(ghost)、「重新生成溯源图谱」(ghost) |
| 含图工具结论被追问 | 「重新生成拓扑/溯源图谱」(主) |

---

## 5. 右栏信息架构

### 统一结构
- 顶部身份条：kicker + 状态结论 + 全局指标chips
- 可折叠分区：kicker标题 + 计数徽标 + 折叠箭头，自动展开最相关分区
- 空状态：图标 + 一句话 + 可点击动作

### 01 体检页（5分区）
1. 扫描状态（五阶段进度，完成后折叠为摘要行）
2. 节点详情（未选中时空状态"在拓扑中选择节点"，含部署蜜点→/在对话中提及）
3. 资产清单（统计chips + 全部/高危过滤 + 小搜索框）
4. 图例与连线（三级连线 + 六类设备，紧凑两列）
5. 最近告警（全局迷你列表3条，点击跳归因分析）

### 02 蜜点页（5分区）
1. 部署态势（总数/四类型分布/节点覆盖率/今日告警触发数）
2. 目标节点/部署控制台（可行性结论 + 四模板2×2网格 + 端口 + 规则说明）
3. 待审批（仅存在待决方案时出现，红色kicker WAITING APPROVAL，双向同步）
4. Agent建议（自主决策大按钮 + 两条带理由建议）
5. 诱饵清单（节点/类型徽标/端口/状态/部署时间/触发告警数/卸载按钮）

### 03 归因页（7分区）
1. 全局态势（待研判/critical/已闭环计数 + 失陷资产 + 活跃诱饵 + 14天迷你趋势图）
2. 告警队列（列表 + 筛选 + 当前分析高亮）
3. 分析进度（L1-L4解锁状态，与AttackGraph严格同步）
4. 证据时间线（观测/推断分级 + 筛选 + 未解锁层锁定）
5. 风险分析（L4后展开：置信度环 + 因子分值条 + 影响范围）
6. MITRE ATT&CK（战术→技术→证据跳转，按层解锁）
7. 处置建议（P0/P1/P2分级 + 生成工单按钮 + 状态流转）

---

## 6. Mock 数据清单

### 告警队列（9条，跨14天 2026-09-06 ~ 2026-09-19）
| ID | 时间 | 蜜点 | 行为 | 级别 | 状态 | campaign |
|---|---|---|---|---|---|---|
| ALERT-2026-0919-001 | 09-19 02:14 | 108 SSH | 爆破+登录成功+命令执行+回连 | critical | 待研判 | C-0919 |
| ALERT-2026-0919-002 | 09-19 02:13 | 105 SMB | SMB爆破47次 | high | 研判中 | C-0919 |
| ALERT-2026-0918-001 | 09-18 14:22 | 107 SQL | svc_scan非维护时段登录+触碰SQL | high | 研判中 | C-0917 |
| ALERT-2026-0917-001 | 09-17 03:05 | 107 SQL | 弱口令尝试+查询敏感表 | high | 待研判 | C-0917 |
| ALERT-2026-0915-001 | 09-15 09:30 | 网关 WEB | 目录扫描+弱口令尝试 | medium | 已闭环 | C-0912 |
| ALERT-2026-0914-001 | 09-14 11:20 | 网关 WEB | 路径遍历探测 | medium | 已闭环 | C-0912 |
| ALERT-2026-0912-001 | 09-12 21:04 | 108 SSH | 前期部署触发测试 | low | 已闭环 | — |
| ALERT-2026-0910-001 | 09-10 16:45 | 网关 WEB | 外网扫描探测 | medium | 已闭环 | C-0912 |
| ALERT-2026-0906-001 | 09-06 08:12 | 103 SMB | 扫描探测 | low | 误报 | — |

### Campaign（3条）
- **C-0919「内部横向移动」** critical 待研判：103 wangwei→105爆破→108 MS17-010横向→SSH诱饵→回连网关
- **C-0912「外网扫描试探」** medium 已闭环：外网IP对网关WEB诱饵目录扫描+弱口令，未突破
- **C-0917「服务账号可疑行为」** high 研判中：svc_scan非维护时段登录107触碰SQL诱饵，证据稀疏置信度62%

### 实体库
- 攻击源：192.168.1.103(跳板,历史2起)、外网模拟203.0.113.45(历史3起)、192.168.1.107(服务账号登录源)
- 账号：wangwei(离职,最后合法登录08-20)、svc_scan(服务账号,维护时段02:00-04:00)、root(108弱口令)
- IOC：文件哈希a1b2c3...、User-Agent "Mozilla/5.0 (compatible; Nmap Scripting Engine)"、回连域名evil-c2.example.com
- TTP：T1110/T1210/T1078/T1046/T1190

---

## 7. 视觉铁律（重申，不得更改）

- 背景 #06080c / 面板 #0b0e14 / 卡体 #080d15 / 强调蓝 #3984ff / 正文 #a1aec2 / 亮色 #e8eefc / 边框 #1e2c45
- 字体 IBM Plex Mono + Noto Sans SC，正文14px/1.8
- AI消息透明无气泡，用户气泡 #15233a
- 蜜点四色：SSH青#22d3ee / SQL琥珀#f59e0b / WEB紫#a78bfa / SMB绿#34d399
- 任何地方不得出现白底
- AI文本只允许1-2句短结论，禁止Markdown表格/ASCII图

## 8. 必须保留不得回退的能力

sessionNonce防刷新重复发送、DeepSeek失败自动回退脚本引擎、键盘1/2/3切页（输入框聚焦不拦截）、每页重放play/free两模式、自动播放AutoPlay、消息内IP可点（CIDR不链接）、跨页发送防重、三页store联动、AttackGraph层数与右栏同步、全局深色兜底。
